{
  "name": "coffee-template",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "node dist/server.js",
    "coffeeWatch": "coffee --compile --transpile --inline-map --no-header --output dist --watch coffee",
    "coffee": "coffee --compile --transpile --inline-map --no-header --output dist coffee"
  },
  "author": "Milad",
  "license": "Apache-2.0",
  "dependencies": {
    "cfenv": "^1.0.x",
    "cloudant": "^1.10.0",
    "body-parser": "^1.18.2",
    "defaults": "^1.0.3",
    "dotenv": "^4.0.0",
    "express": "^4.13.3",
    "vcap_services": "^0.3.4",
    "webpack": "^3.5.5"
  },
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "coffee-loader": "^0.9.0"
  },
  "engines": {
    "node": ">=4"
  }
}
